// ng-mcp-ui/tunnel — cloudflared dev-tunnel manager + SSE control routes.
// Real surface lands in the M4 tunnel-track steps. Skeleton: version marker only.
export { NG_MCP_UI_VERSION } from "../version.js";
