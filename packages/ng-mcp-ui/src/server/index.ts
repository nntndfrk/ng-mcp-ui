// ng-mcp-ui/server — framework-neutral MCP server library.
// Real surface (McpServer, createMcpExpressRouter, view resources, shell + asset
// routers) lands in the M1 server-track steps. Skeleton: version marker only.
export { NG_MCP_UI_VERSION } from "../version.js";
