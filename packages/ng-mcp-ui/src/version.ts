/**
 * Package version, re-exported from every entry point. Kept in sync with
 * package.json#version by hand until release tooling lands (production step S33).
 */
export const NG_MCP_UI_VERSION = "0.0.0";
