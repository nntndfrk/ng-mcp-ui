// ng-mcp-ui/web — Angular host bridge + signal wrappers.
// Real surface (provideMcpUi, bootstrapWidget, the inject* API, DataLlmDirective,
// McpAssetPipe) lands in the M2 web-track steps. Skeleton: version marker only.
export { NG_MCP_UI_VERSION } from "../version.js";
